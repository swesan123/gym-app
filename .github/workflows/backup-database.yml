name: Backup Database

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install tooling
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client rclone

      - name: Validate required secrets
        env:
          SUPABASE_MIGRATION_DB_URL: ${{ secrets.SUPABASE_MIGRATION_DB_URL }}
          GOOGLE_DRIVE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_DRIVE_CREDENTIALS_JSON }}
          GOOGLE_DRIVE_FOLDER_ID: ${{ secrets.GOOGLE_DRIVE_FOLDER_ID }}
        run: |
          if [ -z "$SUPABASE_MIGRATION_DB_URL" ]; then
            echo "Missing secret: SUPABASE_MIGRATION_DB_URL"
            exit 1
          fi
          if [ -z "$GOOGLE_DRIVE_CREDENTIALS_JSON" ]; then
            echo "Missing secret: GOOGLE_DRIVE_CREDENTIALS_JSON"
            exit 1
          fi
          if [ -z "$GOOGLE_DRIVE_FOLDER_ID" ]; then
            echo "Missing secret: GOOGLE_DRIVE_FOLDER_ID"
            exit 1
          fi

      - name: Create SQL backup
        env:
          SUPABASE_MIGRATION_DB_URL: ${{ secrets.SUPABASE_MIGRATION_DB_URL }}
        run: |
          timestamp="$(date -u +%Y%m%d-%H%M%S)"
          backup_file="gym-app-backup-${timestamp}.sql.gz"
          pg_dump "$SUPABASE_MIGRATION_DB_URL" --no-owner --no-privileges | gzip > "$backup_file"
          echo "BACKUP_FILE=$backup_file" >> "$GITHUB_ENV"

      - name: Configure rclone for Google Drive
        env:
          GOOGLE_DRIVE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_DRIVE_CREDENTIALS_JSON }}
          GOOGLE_DRIVE_FOLDER_ID: ${{ secrets.GOOGLE_DRIVE_FOLDER_ID }}
        run: |
          credentials_file="$RUNNER_TEMP/gdrive-service-account.json"
          printf '%s' "$GOOGLE_DRIVE_CREDENTIALS_JSON" > "$credentials_file"
          mkdir -p "$RUNNER_TEMP/rclone"
          cat > "$RUNNER_TEMP/rclone/rclone.conf" <<EOF
          [gdrive]
          type = drive
          scope = drive
          service_account_file = $credentials_file
          root_folder_id = $GOOGLE_DRIVE_FOLDER_ID
          EOF
          echo "RCLONE_CONFIG=$RUNNER_TEMP/rclone/rclone.conf" >> "$GITHUB_ENV"

      - name: Upload backup to Google Drive
        run: rclone copy "$BACKUP_FILE" "gdrive:"

      - name: Upload backup artifact (optional)
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.BACKUP_FILE }}
          path: ${{ env.BACKUP_FILE }}
          retention-days: 14
