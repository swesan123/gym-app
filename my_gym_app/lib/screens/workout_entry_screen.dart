import 'package:flutter/material.dart';

class WorkoutEntryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Workout Entry & History')),
      body: Center(child: Text('This is the Workout Entry Screen')),
    );
  }
}
